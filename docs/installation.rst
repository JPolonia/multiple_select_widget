============
Installation
============

At the command line::

    $ easy_install multiple_select_widget

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv multiple_select_widget
    $ pip install multiple_select_widget
